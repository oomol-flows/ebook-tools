version: 0.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    apt-get update && apt-get install -y calibre
    npm install
    poetry install --no-root
name: ebook-tools
repository: https://github.com/oomol-flows/ebook-tools
releaseNotes: init
displayName: "%e-book-tools%"
icon: ./icon.png
description: "%epub-mobi-azw3-pdf-calibre%"
